[package]
name = "day6"
version.workspace = true
edition.workspace = true

[dependencies]
eyre.workspace = true
utils = { workspace = true, optional = true }

[features]
default = ["utils"]
utils = ["dep:utils"]
